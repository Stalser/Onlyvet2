import BookingWidget from '@/components/BookingWidget';
export const metadata = { title: 'Запись — OnlyVet' };
export default function BookingPage() {
  return (<section className="container py-16"><BookingWidget /></section>);
}
